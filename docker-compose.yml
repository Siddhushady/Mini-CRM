version: '3.8'

services:
  crm:
    build: .
    container_name: mini_crm
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=sqlite:///mini_crm.db
    volumes:
      - .:/app
      - ./mini_crm.db:/app/mini_crm.db
    restart: unless-stopped
